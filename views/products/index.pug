extends ../layout

block content
  section
    ul
      each product in products
        li
          a(href=`/products/${product.id}`)
            h3= product.name
          p= `\$ ${product.price}`
